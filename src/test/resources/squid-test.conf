http_port 3128

# Allow all ports (WireMock uses a dynamic port assigned at test startup)
acl Safe_ports port 1-65535
acl CONNECT method CONNECT
acl all src all

http_access allow all

# No caching in tests
cache deny all
cache_mem 0 MB

access_log /var/log/squid/access.log squid
